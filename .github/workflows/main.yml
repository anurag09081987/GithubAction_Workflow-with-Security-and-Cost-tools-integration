name : CI

on:
  pull_request:
    branches:
      - main

permissions:
  id-token: write
  contents: read



jobs:
  terraform_workflow:
    name: scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7
